/**
 * Worker tasks passed to handle() method can have any structure.
 * When you create new type of worker, describe its task's structure
 * with interface inherited from WorkerTask
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface WorkerTask {}
